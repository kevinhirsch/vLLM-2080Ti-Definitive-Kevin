{
  "id": "code_exec_009",
  "category": "code_exec",
  "description": "word_frequency",
  "request": {
    "messages": [
      {
        "role": "user",
        "content": "Write a Python function `word_frequency(text)` that returns a dict mapping each lowercased word (split on whitespace, punctuation stripped from word edges) to its occurrence count.\n\nReply with a single Python code block containing only your function definition(s) (imports allowed if needed). Do not include any example usage, test calls, or print statements of your own -- a separate test harness will be run against your function afterward. Do not print anything else outside the code block."
      }
    ],
    "max_tokens": 900
  },
  "check": {
    "type": "code_exec",
    "expected_stdout": "[]\n[('cat', 1), ('mat', 1), ('on', 1), ('sat', 1), ('the', 2)]\n[('hello', 3), ('world', 1)]\n",
    "timeout_sec": 10,
    "harness": "for text in ['', 'the cat sat on the mat', 'Hello, hello! HELLO world.']:\n    d = word_frequency(text)\n    print(sorted(d.items()))"
  }
}
