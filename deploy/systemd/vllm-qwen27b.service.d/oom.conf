# Added 2026-07-17: protect vLLM from the OOM killer.
# systemd applies this to the main process and all children in the
# service cgroup, on every (re)start. Under memory pressure the kernel
# will kill other processes (e.g. Chrome tabs) before this inference server.
[Service]
OOMScoreAdjust=-800
