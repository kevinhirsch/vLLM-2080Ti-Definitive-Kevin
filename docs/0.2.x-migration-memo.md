# 0.2.x migration memo (M-2) — DRAFT go/no-go

**Verdict (draft, pending r6/FP8/research inputs): staged GO.** Upstream's own
same-silicon A/B shows the v0.27.1 rebase is worth +4.8% prefill / **+24.9%
decode** at MTP3 (their 2080 Ti pair, official FP8 route: 60.7 → 75.8 tok/s),
and 0.2.x unlocks the checkpoint routes we cannot run today (NVFP4 fast route
119.3, AWQ-INT4 ~102, INT8 W8A8 2525 prefill). The 0.1.x line stays the
maintained stable fallback per upstream — cutover only after our full gate.

## Environment delta (serving box HNET00)

| | today (0.1.x) | 0.2.1-pre2 target |
| --- | --- | --- |
| vLLM base | 0.21-era | v0.27.1 V1 contracts |
| CUDA / ABI | 12.8 | 13.0 / cu130 |
| PyTorch / Python | 2.11 / 3.11 | 2.13 / 3.12 |
| Host reqs | met | Ubuntu 26.04+/kernel 7+/GCC 15 — **kernel ✓ (7.0.0-28), verify GCC 15 + CUDA 13 install** |

Build as a **parallel venv + separate checkout** — prod 0.1.x untouched until
the cutover gate passes. Windows use the existing hardened driver.

## Our fork-added work: per-patch disposition on 0.2.x

| # | Ours | 0.2.x status | Action |
| --- | --- | --- | --- |
| 1 | #111 spec-verify workspace reserve (merged up) | "Migrated" per their audit | ✅ nothing |
| 2 | #125 named-tool streaming (merged up) | #98 "Migrated and tested" | ✅ nothing |
| 3 | #131 DP-aware last-stage mask (open) | reserve exists; DP-mask fix likely still needed | re-check, small re-port |
| 4 | #133 offload align hit-boundary (open) | offload "absorbed by current paths" | verify the non-divisor bug exists there; port test first |
| 5 | MTP align retention (M-3, env-gated) | scheduler/align integrated differently | re-derive invariant on new scheduler; medium |
| 6 | S4 scoped-reemission drafter (flagship) | no equivalent | full re-port; LARGEST item |
| 7 | align-allocator tolerate fix (10da324) | new hybrid cache code | check if assert exists at all; small |
| 8 | vllm#51508 GDN stale-row port (b2e84e2) | v0.27.1 does NOT contain it (PR still open) | re-port; small-medium |
| 9 | TQ k3v4_nc (3-bit K validated) | TQ reworked (#71/#78 rows); TQ4NC present | verify k3 variant exists; port if not |
| 10 | 524K YaRN + lane/pool config | config-level | trivial |
| 11 | K=3 + capless MTP (today) | config-level; check their MTP3 defaults | trivial |
| 12 | EXP-046 varlen loop (env-gated, off) | 0.2.x keeps Triton/FLA fallback for unsupported varlen | likely obsolete ✅ |
| 13 | Gateway/shim + ops tooling | engine-independent | ✅ nothing |
| 14 | Vision 1MP processor ceiling | newer mm stack; re-measure ceiling | small re-validation |
| 15 | M-7 cap/QWOPUS env bugs (today) | envs likely absent in 0.2.x | verify absent = obsolete ✅ |

Bottom line: 2 large items (S4 re-port, retention re-derive), ~5 small ports,
rest free or trivial. Estimate: 2-3 focused window-days, engine risk isolated
to windows.

## Staged plan
1. Parallel env build (GCC 15 + cu130 toolchain check first) + 0.2.x checkout; smoke-boot GPTQ-Int4 on a window.
2. Same-shape C1-C4 bench + evalkit vs today's numbers → **answer their testing call with our results either way** (we own reference hardware).
3. FP8 + NVFP4 + W8A8 checkpoint routes benched (the 119.3 target).
4. Port wave per the table; S4 last.
5. Cutover gate: evalkit 44/45 + C1/C2 ≥ 0.1.x numbers + 524K (or accepted new ceiling) + aged-engine bench + tool/vision smokes. 0.1.x bank retained for instant rollback.

## Open questions feeding the final verdict
- ~~GCC 15 / CUDA 13 availability on HNET00~~ **RESOLVED 2026-08-24: GCC 15.2.0 installed, Python 3.12 via uv, kernel 7.0.0-28, driver 595.84 supports CUDA 13.2 — no driver change needed. Remaining: side-by-side CUDA 13 toolkit install (/usr/local/cuda-13.x; prod untouched, uses driver + venv runtime). Migration is a build-only environment change.**
- TQ k3v4_nc parity on 0.2.x (pool math depends on it).
- Deep-research report (job 20260824-145309) may add community 0.2.x field reports.
